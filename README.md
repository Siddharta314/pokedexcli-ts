# Pokedex Cli in TS

A lightweight, interactive Command Line Interface (CLI) built with TypeScript and Node.js. This application uses a custom REPL (Read-Eval-Print Loop) to fetch and display data from the PokeAPI.
